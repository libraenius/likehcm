# Отчет об анализе проекта на неиспользуемый код

## 🔴 Критичные проблемы

### 1. Неиспользуемые зависимости в package.json

#### `@hookform/resolvers` - НЕ ИСПОЛЬЗУЕТСЯ
- **Статус**: Установлена, но нигде не импортируется
- **Размер**: ~50KB
- **Рекомендация**: Удалить, если не планируется интеграция react-hook-form с валидацией через zod

#### `react-hook-form` - ЧАСТИЧНО ИСПОЛЬЗУЕТСЯ
- **Статус**: Используется только в `src/components/ui/form.tsx`
- **Проблема**: Компонент Form не используется нигде в проекте
- **Рекомендация**: 
  - Либо удалить `form.tsx` и `react-hook-form` + `@hookform/resolvers`
  - Либо интегрировать react-hook-form в существующие формы (как указано в IMPROVEMENTS.md)

### 2. Неиспользуемые компоненты UI

#### `src/components/ui/form.tsx` - НЕ ИСПОЛЬЗУЕТСЯ
- Компонент полностью реализован, но нигде не импортируется
- Зависит от `react-hook-form`

#### `src/components/ui/radio-group.tsx` - НЕ ИСПОЛЬЗУЕТСЯ
- Компонент реализован, но нигде не используется
- Зависит от `@radix-ui/react-radio-group`
- **Примечание**: Используется только внутри `dropdown-menu.tsx` как `DropdownMenuRadioGroup`, но сам `RadioGroup` не используется

#### `src/components/ui/slider.tsx` - НЕ ИСПОЛЬЗУЕТСЯ
- Компонент реализован, но нигде не используется
- Зависит от `@radix-ui/react-slider`

#### `src/components/ui/navigation-menu.tsx` - НЕ ИСПОЛЬЗУЕТСЯ
- Компонент полностью реализован, но нигде не используется
- Зависит от `@radix-ui/react-navigation-menu`

### 3. Дублирующиеся хуки

#### `src/hooks/use-toast.ts` - ДУБЛИКАТ
- **Проблема**: Существует два варианта хука `useToast`:
  - `src/hooks/use-toast.ts` - НЕ ИСПОЛЬЗУЕТСЯ
  - `src/contexts/toast-context.tsx` - ИСПОЛЬЗУЕТСЯ (правильный вариант)
- **Рекомендация**: Удалить `src/hooks/use-toast.ts`

#### `src/hooks/use-debounce.ts` - НЕ ИСПОЛЬЗУЕТСЯ
- Хук реализован, но нигде не используется
- **Рекомендация**: Удалить, если не планируется использование

### 4. Неиспользуемые сервисы

#### `src/lib/services/profile-service.ts` - НЕ ИСПОЛЬЗУЕТСЯ
- Все функции сервиса (`getAllProfiles`, `getProfile`, `createProfileService`, `updateProfileService`, `deleteProfileService`) не используются
- Проект использует прямые вызовы функций из `reference-data.ts` через `data.ts`
- **Рекомендация**: Удалить или интегрировать в проект

#### `src/lib/services/competence-service.ts` - НЕ ИСПОЛЬЗУЕТСЯ
- Все функции сервиса (`getAllCompetences`, `getCompetence`, `createCompetenceService`, `updateCompetenceService`, `deleteCompetenceService`) не используются
- Проект использует прямые вызовы функций из `reference-data.ts` через `data.ts`
- **Рекомендация**: Удалить или интегрировать в проект

### 5. Неиспользуемые файлы и ресурсы

#### `src/app/globals-green-theme-example.css` - ПРИМЕР
- Файл-пример, не используется в проекте
- **Рекомендация**: Оставить как документацию или удалить

#### `src/app/color-schemes.css` - НЕ ИСПОЛЬЗУЕТСЯ
- Файл с вариантами цветовых схем, но не импортируется
- **Рекомендация**: Оставить как документацию или удалить

#### `src/app/evaluation/` - ПУСТАЯ ДИРЕКТОРИЯ
- Пустая директория без файлов
- **Рекомендация**: Удалить

#### Файлы в `public/` - ЧАСТИЧНО ИСПОЛЬЗУЮТСЯ
- ✅ `cheburashka.png` - используется в `app-sidebar.tsx`
- ❌ `file.svg` - не используется
- ❌ `globe.svg` - не используется
- ❌ `next.svg` - не используется (стандартный Next.js файл)
- ❌ `vercel.svg` - не используется (стандартный Next.js файл)
- ❌ `window.svg` - не используется

### 6. Неиспользуемые шрифты

#### `Geist_Mono` - ЧАСТИЧНО ИСПОЛЬЗУЕТСЯ
- Шрифт загружается в `layout.tsx`, но переменная `--font-geist-mono` не используется в CSS
- Используется только `--font-geist-sans`
- **Рекомендация**: Удалить загрузку `Geist_Mono`, если не планируется использование моноширинного шрифта

## 🟡 Потенциально неиспользуемые зависимости

### Radix UI компоненты (используются только внутри UI компонентов, но сами компоненты не используются):
- `@radix-ui/react-radio-group` - используется только в `radio-group.tsx`, который не используется
- `@radix-ui/react-slider` - используется только в `slider.tsx`, который не используется
- `@radix-ui/react-navigation-menu` - используется только в `navigation-menu.tsx`, который не используется

## 📊 Статистика

- **Неиспользуемых зависимостей**: 2 (`@hookform/resolvers`, частично `react-hook-form`)
- **Неиспользуемых компонентов UI**: 4 (`form`, `radio-group`, `slider`, `navigation-menu`)
- **Неиспользуемых хуков**: 2 (`use-toast.ts`, `use-debounce.ts`)
- **Неиспользуемых сервисов**: 2 (`profile-service.ts`, `competence-service.ts`)
- **Неиспользуемых файлов**: ~8 (CSS примеры, SVG файлы, пустые директории)

## ✅ Рекомендации по очистке

### Высокий приоритет (можно удалить сразу):
1. Удалить `src/hooks/use-toast.ts` (дубликат)
2. Удалить `src/hooks/use-debounce.ts` (не используется)
3. Удалить `src/lib/services/profile-service.ts` (не используется)
4. Удалить `src/lib/services/competence-service.ts` (не используется)
5. Удалить `src/app/evaluation/` (пустая директория)
6. Удалить неиспользуемые SVG файлы из `public/`

### Средний приоритет (требует решения):
1. **Вариант A**: Удалить `react-hook-form`, `@hookform/resolvers` и `form.tsx`
2. **Вариант B**: Интегрировать react-hook-form в существующие формы
3. Удалить неиспользуемые UI компоненты (`radio-group`, `slider`, `navigation-menu`) и их зависимости
4. Удалить `Geist_Mono` из `layout.tsx`, если не планируется использование

### Низкий приоритет (документация):
1. Оставить или удалить примеры CSS файлов (`globals-green-theme-example.css`, `color-schemes.css`)
2. Оставить или удалить документацию (`APPLY_COLOR_SCHEME.md`, `COLOR_SCHEMES.md`)

## 💾 Потенциальная экономия

- **Размер bundle**: ~200-300KB (после удаления неиспользуемых зависимостей и компонентов)
- **Упрощение кодовой базы**: ~500-700 строк неиспользуемого кода
- **Улучшение поддерживаемости**: Меньше файлов для поддержки

